projection;
strict ( 2 );
use draft;

define behavior for ZC_RAP_TRAVEL_00
use etag

{


//field ( readonly : update ) CustomerID, BeginDate, EndDate;
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}